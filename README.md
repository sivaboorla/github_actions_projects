#script agenda
#Task1 - This is the task to fetch all repositories from organization or user and then store it in a json file.
#Task2 - Take the json results are input of the file and then extract values of each repository.
#Task3 - This is task to push extracted results into external source like any kind of database sql db then check those results as repositry level.
